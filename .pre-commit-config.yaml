repos:
  - repo: local
    hooks:
      - id: trufflehog-filesystem
        name: TruffleHog (pre-commit: filesystem/HEAD, BLOCK)
        entry: trufflehog filesystem .
        language: system
        stages: [commit]
