repos:
  - repo: local
    hooks:
      - id: trufflehog
        name: TruffleHog (verified secrets only)
        entry: >
          trufflehog git file://.
          --since-commit HEAD
          --only-verified
          --fail
        language: system
        stages: [commit, push]
